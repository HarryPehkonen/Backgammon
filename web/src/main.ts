/**
 * App entry point.
 *
 * The whole UI is one custom element, so booting the app is just a matter of
 * defining it — `index.html` already has the `<backgammon-board>` tag waiting.
 */
import "./index.css";
import "./backgammon-board.ts";
